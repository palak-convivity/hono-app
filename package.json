{
  "name": "hono-app",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.9.12"
  },
  "devDependencies": {
    "@types/node": "^24.9.1",
    "wrangler": "^4.4.0"
  }
}